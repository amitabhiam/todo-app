import React from 'react';
import { useState } from 'react';

function Sample() {
    const [data, setData] = useState(0);

    return (
    <div>
        <h1>This is the sample components files</h1>
    </div>
    )
}

export default Sample;